name: CI Branch - Roboshop Catalogue
on:
  push:
    branches-ignore:
      - main
    paths-ignore:
      - 'db/**'
      - '.github/workflows/ci-db-main.yml'

jobs:
  cicd:
    uses: roboshopDevsecops/github-reusable-workflows/.github/workflows/ci-b.yml@main
